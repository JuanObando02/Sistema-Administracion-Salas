@using Microsoft.AspNetCore.Identity
<!DOCTYPE html>
<html lang="en">
@{
    // Ocultar sidebar solo en la página "Bienvenido"
    var showSidebar = ViewData["Title"]?.ToString() != "Bienvenido";
    var hideSidebar = ViewData["Title"]?.ToString() == "Login" || ViewData["Title"]?.ToString() == "Register";
}

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Sistema Gestor de Salas</title>
    <link rel="icon" type="image/x-icon" href="~/favicon.ico?v=3" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/MvcSample.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body class="@(hideSidebar ? "no-sidebar" : "")">
    @if (hideSidebar)
    {
        <div class="d-flex justify-content-between align-items-center p-3 bg-light border-bottom">
            <a class="navbar-brand fw-bold fs-3" style="color: black;"
               asp-area="" asp-controller="Home" asp-action="Index">
                SalasSys
            </a>
            <div>
                <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-primary me-2">Iniciar Sesión</a>
                <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-outline-secondary">Registrarse</a>
            </div>
        </div>
    }


    @if(showSidebar) {
    <header>
        <nav class="navbar navbar-light bg-white border-end vh-100 position-fixed flex-column p-3">
            <a class="navbar-brand mb-4 text-center" asp-area="" asp-controller="Home" asp-action="Index">SalasSys</a>

            <ul class="nav nav-pills flex-column mb-auto">
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                @* --- BLOQUE DE ADMINISTRADOR --- *@
                @if (User.IsInRole("Admin") || User.IsInRole("Coordinador"))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdownAdmin" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Administración
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdownAdmin">
                            <li>
                                <a class="dropdown-item" asp-area="" asp-controller="Salas" asp-action="Index">Gestión de Salas</a>
                            </li>
                            <li>
                                <a class="dropdown-item" asp-area="" asp-controller="Equipos" asp-action="Index">Gestión de Equipos</a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item fw-bold text-primary" asp-controller="Reservas" asp-action="Gestionar">
                                    <i class="bi bi-calendar-check"></i> Aprobar Reservas
                                </a>
                            </li>
                            @if (User.IsInRole("Admin"))
                            {
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <a class="dropdown-item" asp-area="" asp-controller="Usuarios" asp-action="Index">Gestión de Usuarios</a>
                                </li>
                                <li>
                                    <a class="dropdown-item" asp-area="" asp-controller="Roles" asp-action="Index">Gestión de Roles</a>
                                </li>
                            }
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-danger fw-bold" asp-area="" asp-controller="Reportes" asp-action="Registrar">
                            <i class="bi bi-exclamation-triangle"></i> Reportar Daño
                        </a>
                    </li>
                }
                @* --- ENLACES PARA TODOS (Logueados) --- *@
                @if (User.Identity.IsAuthenticated)
                {
                    if (!User.IsInRole("Admin") && !User.IsInRole("Coordinador"))
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Reservas" asp-action="Index">Mis Reservas</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-controller="Reportes" asp-action="MisReportes">Mis Reportes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-controller="Asesorias" asp-action="Index">Asesorías</a>
                        </li>
                    }
                }
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                </li>
            </ul>

            <ul class="nav flex-column mt-auto">
                @if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <span class="nav-link text-dark">Hola, @User.Identity.Name!</span>
                    </li>
                    <li class="nav-item">
                        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout"
                              asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                            <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                        </form>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
                    </li>
                }
            </ul>
        </nav>
    </header>
    }
    @if (!showSidebar)
    {
        <div class="d-flex justify-content-end p-3 bg-light border-bottom">
            <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-primary me-2">Iniciar Sesión</a>
            <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-outline-secondary">Registrarse</a>
        </div>
    }

    <div class="flex-grow-1 p-3" style="@(showSidebar ? "margin-left:250px;" : "")">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
